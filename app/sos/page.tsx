// Server component
import SosPage from './SosClient';

export const metadata = {
  title: 'Emergency Reporting – Yindee Hub',
  description: 'Report emergencies or contact Tourist Police through the official app.',
};

export default function Page() {
  return <SosPage />;
}
